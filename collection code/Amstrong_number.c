#include<stdio.h>
main()
{
	int n,sum=0,r,num,arm;
	printf("Enter the number:\n");
	scanf("%d",&n);
	while (n>0)
	{
		r=n%10;
		sum=r*r*r;
		num=num+c;
		num=arm;
	}
	if (arm=num)
	{
		printf("It is Amstrong number.");
	}
	else 
	printf("It is not Amstrong number.");
}
